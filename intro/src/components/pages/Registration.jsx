// Importing necessary libraries, components, and utilities
import { RegisterForm } from "../access_panel_components/RegisterForm";
import "../../index.css";

/**
 * Registration Page component.
 * Page for the user to access Eventorium with their credentials for the first time
 *
 * Subcomponents:
 * @see RegisterForm
 */
export function Registration() {
  return <RegisterForm />;
}
